export interface StatusCodeUsuario {
  statusCode: number;
  nome: string;
  idUsuario: string;
  nivel_acesso: number;
  email: string;
}
export interface StatusCodeCliente {
  statusCode: number;
  nome: string;
  idCliente: string;
  nivel_acesso: number;
  email: string;
}
