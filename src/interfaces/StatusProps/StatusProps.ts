export interface StatusCodeUsuario {
    statusCode: number
    nome:string
    idUsuario:string
    nivel_acesso: number
}
export interface StatusCodeCliente {
    statusCode: number
    nome:string
    idCliente:string
    nivel_acesso: number
}